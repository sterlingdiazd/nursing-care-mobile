# EXPO_PUBLIC_API_BASE_URL is optional - leave empty or unset for automatic IP detection
# When running on physical devices, the app will auto-detect the dev machine's IP
#
# Only set this if you want to explicitly override the API endpoint:
# EXPO_PUBLIC_API_BASE_URL=https://192-168-1-100.sslip.io:5050
